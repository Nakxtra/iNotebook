import React from 'react'

const About = () => {
    // const a = useContext(NoteContext);

    return (
        <div>
            {/* My name is {a.state.name} and my branch is {a.state.branch} */}
            This is About page
        </div>
    )
}

export default About
